/**
 * Represents the properties for a component that receives a column.
 * @interface
 */
interface ColumnProps {
  /**
   * The column object containing information about the column.
   * @member {Column}
   */
  column: Column;
}
